interface:
  display_name: "Cascade Prompt"
  short_description: "Build reliable AI prompts"
  default_prompt: "Use $prompt to build a reliable task-specific prompt, load only relevant context and policies, resolve material gaps, and propose bounded validation."
policy:
  allow_implicit_invocation: true
