interface:
  display_name: "Accessibility Review"
  short_description: "Audit accessible interaction and evidence."
  default_prompt: "Use $accessibility-review to review semantics, names, keyboard and focus behavior, contrast, target size, forms, status messages, motion, and mobile access without claiming legal compliance."
policy:
  allow_implicit_invocation: true
