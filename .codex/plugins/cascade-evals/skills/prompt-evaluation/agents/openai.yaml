interface:
  display_name: "Prompt Evaluation"
  short_description: "Evaluate prompts through bounded simulations"
  default_prompt: "Use $prompt-evaluation to run a versioned prompt-quality or adaptive-interview campaign through bounded agent-response simulations."
policy:
  allow_implicit_invocation: true
