interface:
  display_name: "Execute Simulation Campaign"
  short_description: "Execute approved simulation campaigns safely"
  default_prompt: "Use $execute-simulation-campaign to execute one approved simulation campaign, freeze its evidence, verify cleanup, and produce an execution receipt."
