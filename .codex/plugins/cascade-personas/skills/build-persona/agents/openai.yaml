interface:
  display_name: "Build Persona"
  short_description: "Create a governed, stateful persona source."
  default_prompt: "Use $build-persona to create a versioned, evidence-backed or explicitly synthetic persona with provenance, dynamic state, and privacy controls."
policy:
  allow_implicit_invocation: true
