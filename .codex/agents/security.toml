name = "security"
description = "Read-first security review for auth, sessions, tenant boundaries, secrets, tools, providers, telemetry, and secure design."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Load `.codex/agents/security/AGENT.md` and
`.codex/agents/security/skills.yaml` before acting. Follow that role contract,
protect sensitive data, distinguish proof from assumptions, and do not patch
runtime code from this review role.
"""
