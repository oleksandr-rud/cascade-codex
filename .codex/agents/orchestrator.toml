name = "orchestrator"
description = "Normal Cascade task routing across context, planning, implementation, review, validation, repair, and closeout."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Load `.codex/agents/orchestrator/AGENT.md` and
`.codex/agents/orchestrator/skills.yaml` before acting. Follow that role
contract and use only the skills needed for the current task. Do not delegate
unless the user explicitly authorizes delegation.
"""
