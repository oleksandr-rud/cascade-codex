{
  "schema_id": "architecture-scaffold-profiles-v1",
  "description": "Minimal source structures generated only after a concrete domain module and technology profile are selected.",
  "rules": {
    "default_mode": "preview",
    "overwrite": false,
    "install_dependencies": false,
    "run_commands": false,
    "required_placeholders": [
      "__APP_NAME__",
      "__MODULE_NAME__"
    ]
  },
  "profiles": [
    {
      "profile_id": "backend-bun",
      "description": "Minimal Bun/Hono modular monolith with one concrete domain module and app-owned startup.",
      "pair_ids": [
        "service-api-worker",
        "stack-selection",
        "app-stack",
        "backend-stack"
      ],
      "technology_node": "bun-hono",
      "structure_markers": [
        "src/__APP_NAME__/startup/",
        "src/__APP_NAME__/modules/__MODULE_NAME__/"
      ],
      "files": {
        "ARCHITECTURE.md": "# __APP_NAME__ architecture\n\nAdopted defaults: `service-api-worker`, `stack-selection`, `app-stack`, and `backend-stack:bun-hono`.\n\nThis application is a modular monolith with one release boundary. `src/__APP_NAME__/startup` composes the process. `src/__APP_NAME__/modules/__MODULE_NAME__` owns its state, behavior, and public entrypoint. Other modules call its public contract in-process, never its storage. Add modules for real domain capabilities; add transports or shared technical code only when current requirements prove the need.\n",
        "src/__APP_NAME__/startup/main.ts": "import { Hono } from \"hono\";\nimport { create__MODULE_NAME_PASCAL__Module } from \"../modules/__MODULE_NAME__\";\n\nconst app = new Hono();\nconst __MODULE_NAME_CAMEL__ = create__MODULE_NAME_PASCAL__Module();\napp.route(\"/__MODULE_NAME__\", __MODULE_NAME_CAMEL__.routes);\n\nexport default app;\n",
        "src/__APP_NAME__/modules/__MODULE_NAME__/index.ts": "import { Hono } from \"hono\";\n\nexport function create__MODULE_NAME_PASCAL__Module() {\n  const routes = new Hono();\n  return { routes };\n}\n"
      }
    },
    {
      "profile_id": "backend-go",
      "description": "Minimal Go modular monolith with one concrete domain package and explicit startup boundary.",
      "pair_ids": [
        "service-api-worker",
        "stack-selection",
        "app-stack",
        "backend-stack"
      ],
      "technology_node": "go-standard-library",
      "structure_markers": [
        "src/__APP_NAME__/startup/",
        "src/__APP_NAME__/modules/__MODULE_NAME__/"
      ],
      "files": {
        "ARCHITECTURE.md": "# __APP_NAME__ architecture\n\nAdopted defaults: `service-api-worker`, `stack-selection`, `app-stack`, and `backend-stack:go-standard-library`.\n\nThis application is a modular monolith with one release boundary. `src/__APP_NAME__/startup` owns process composition. `src/__APP_NAME__/modules/__MODULE_NAME__` owns its state, behavior, and public package API. Other modules call that API in-process, never its storage. Initialize the target Go module before wiring imports. Do not create generic service, common, or repository-base packages.\n",
        "cmd/__APP_NAME__/main.go": "package main\n\n// Wire the target module path to startup.Run after go.mod exists.\nfunc main() {}\n",
        "src/__APP_NAME__/startup/run.go": "package startup\n\n// Run is the process composition boundary. Register concrete domain modules\n// here after the target Go module path and runtime dependencies are selected.\nfunc Run() error {\n\treturn nil\n}\n",
        "src/__APP_NAME__/modules/__MODULE_NAME__/module.go": "package __MODULE_NAME_GO__\n\ntype Module struct{}\n\nfunc New() *Module {\n\treturn &Module{}\n}\n"
      }
    },
    {
      "profile_id": "backend-fastapi",
      "description": "Minimal FastAPI modular monolith with one concrete domain module and app-owned startup.",
      "pair_ids": [
        "service-api-worker",
        "stack-selection",
        "app-stack",
        "backend-stack"
      ],
      "technology_node": "python-fastapi",
      "structure_markers": [
        "src/__APP_NAME__/startup/",
        "src/__APP_NAME__/modules/__MODULE_NAME__/"
      ],
      "files": {
        "ARCHITECTURE.md": "# __APP_NAME__ architecture\n\nAdopted defaults: `service-api-worker`, `stack-selection`, `app-stack`, and `backend-stack:python-fastapi`.\n\nThis application is a modular monolith with one release boundary. `src/__APP_NAME__/startup` owns FastAPI composition. `src/__APP_NAME__/modules/__MODULE_NAME__` owns its state, behavior, and public router. Other modules use its public in-process contract, never loopback HTTP or its storage. Add persistence, providers, events, or shared technical code only when accepted behavior requires them.\n",
        "src/__APP_NAME__/__init__.py": "",
        "src/__APP_NAME__/startup/__init__.py": "",
        "src/__APP_NAME__/startup/main.py": "from fastapi import FastAPI\n\nfrom __APP_NAME__.modules.__MODULE_NAME__ import router as __MODULE_NAME_SNAKE___router\n\napp = FastAPI()\napp.include_router(__MODULE_NAME_SNAKE___router, prefix=\"/__MODULE_NAME__\", tags=[\"__MODULE_NAME__\"])\n",
        "src/__APP_NAME__/modules/__MODULE_NAME__/__init__.py": "from fastapi import APIRouter\n\nrouter = APIRouter()\n\n__all__ = [\"router\"]\n"
      }
    },
    {
      "profile_id": "frontend-react-vite",
      "description": "Minimal React/Vite client with one concrete product feature and a thin app shell.",
      "pair_ids": [
        "web-frontend",
        "stack-selection",
        "app-stack",
        "frontend-stack"
      ],
      "technology_node": "react-vite",
      "structure_markers": [
        "src/app/",
        "src/features/__MODULE_NAME__/"
      ],
      "files": {
        "ARCHITECTURE.md": "# __APP_NAME__ frontend architecture\n\nAdopted defaults: `web-frontend`, `stack-selection`, `app-stack`, and `frontend-stack:react-vite`.\n\n`src/app` composes the client. `src/features/__MODULE_NAME__` owns one concrete product capability. Add shared UI, API, state, or utility code only after at least two current features need the same stable mechanism.\n",
        "src/main.tsx": "import React from \"react\";\nimport ReactDOM from \"react-dom/client\";\nimport { App } from \"./app/App\";\n\nReactDOM.createRoot(document.getElementById(\"root\")!).render(<React.StrictMode><App /></React.StrictMode>);\n",
        "src/app/App.tsx": "import { __MODULE_NAME_PASCAL__Page } from \"../features/__MODULE_NAME__\";\n\nexport function App() {\n  return <__MODULE_NAME_PASCAL__Page />;\n}\n",
        "src/features/__MODULE_NAME__/index.tsx": "export function __MODULE_NAME_PASCAL__Page() {\n  return <main><h1>__MODULE_NAME_TITLE__</h1></main>;\n}\n"
      }
    },
    {
      "profile_id": "frontend-nextjs",
      "description": "Minimal Next.js client with one concrete product feature and thin App Router composition.",
      "pair_ids": [
        "web-frontend",
        "stack-selection",
        "app-stack",
        "frontend-stack"
      ],
      "technology_node": "nextjs",
      "structure_markers": [
        "src/app/",
        "src/features/__MODULE_NAME__/"
      ],
      "files": {
        "ARCHITECTURE.md": "# __APP_NAME__ frontend architecture\n\nAdopted defaults: `web-frontend`, `stack-selection`, `app-stack`, and `frontend-stack:nextjs`.\n\n`src/app` is a thin routing and composition shell. `src/features/__MODULE_NAME__` owns one concrete product capability. Add client state, data gateways, shared UI, or cache policy only when that behavior is selected.\n",
        "src/app/layout.tsx": "import type { ReactNode } from \"react\";\n\nexport default function RootLayout({ children }: { children: ReactNode }) {\n  return <html lang=\"en\"><body>{children}</body></html>;\n}\n",
        "src/app/page.tsx": "import { __MODULE_NAME_PASCAL__Page } from \"../features/__MODULE_NAME__\";\n\nexport default function Page() {\n  return <__MODULE_NAME_PASCAL__Page />;\n}\n",
        "src/features/__MODULE_NAME__/index.tsx": "export function __MODULE_NAME_PASCAL__Page() {\n  return <main><h1>__MODULE_NAME_TITLE__</h1></main>;\n}\n"
      }
    }
  ]
}
