required_bindings:
  - exact device id and platform version
  - exact fixture app id and build
  - named clean snapshot
  - network-denied action runner
  - screenshot and log export
  - snapshot restore verification
schema_version: 1
status: PROVIDER_REQUIRED
